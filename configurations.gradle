configurations {
    includeJar {
        canBeResolved = true
        canBeConsumed = false
        transitive = false
    }
    includeRecursiveJar {
        canBeResolved = true
        canBeConsumed = false
    }
    implementation.extendsFrom(includeJar)
    implementation.extendsFrom(includeRecursiveJar)
}
